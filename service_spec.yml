service_name: some-service

lint:
  target: lint

test:
  target: test

build:
  target: build

environments:
- name: dev
  tier: 3
  containers:

  regions:
    service_distribution:
      type: s3

- name: stage
  tier: 2

- name: prod
  tier: 1

